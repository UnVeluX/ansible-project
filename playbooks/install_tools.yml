---
- name: Install common tools
  hosts: all
  become: yes

  tasks:

    - name: Install packages
      apt:
        name:
          - vim
          - nano
          - curl
          - git
          - htop
          - net-tools
        state: present
      when: ansible_os_family == "Debian"
